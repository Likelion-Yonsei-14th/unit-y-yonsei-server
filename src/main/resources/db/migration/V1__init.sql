-- ============================================================================
-- V1__init.sql : 베이스라인 마이그레이션 (Flyway 시작점)
-- ============================================================================
-- 이 파일은 의도적으로 빈 베이스라인이다.
-- 실제 도메인 스키마(booth, reservation, performance 등)는 각 도메인 PR에서
-- V2__create_booth_table.sql 같은 형태로 추가된다.
--
-- 역할:
--   1. Flyway 가 첫 실행 시 flyway_schema_history 테이블을 생성하도록 트리거
--   2. 마이그레이션 디렉토리(src/main/resources/db/migration/)의 표준 자리 확보
--
-- 작성 규칙 (이후 추가될 마이그레이션 파일에도 동일 적용):
--   - 파일명: V{버전}__{스네이크_케이스_설명}.sql
--   - 한 번 머지된 파일은 절대 수정 금지 (적용된 환경에서 다시 안 돌아감)
--   - 변경이 필요하면 새 V 파일을 추가 (예: V5__add_email_to_booth.sql)
--   - SQL 은 멱등성을 가정하지 않아도 됨 — Flyway 가 적용 여부를 추적
-- ============================================================================

SELECT 1;  -- placeholder. SQL 한 줄 이상 있어야 일부 환경에서 정상 처리됨.
